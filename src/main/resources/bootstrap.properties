# enable Parameter Store as a source
spring.cloud.aws.parameterstore.enabled=true

# match exactly your SSM prefix
spring.cloud.aws.parameterstore.prefix=/instiapp/dev

# pin the region if needed
spring.cloud.aws.region.static=us-east-1
